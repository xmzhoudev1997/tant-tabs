import { TABS_NAV } from 'xm-tabs/nav/props';
import Render from './render';
import React from 'react';

export default (
    props: TABS_NAV,
) => {
    return (
        <Render
            props={props}
        />
    );
}